export interface UserLogged {
  id: number;         // ID del residente propietario
  fullname: string;   // Nombre completo del usuario
  role: string;       // Tipo de usuario 
  house: string;      // Numero de casa
}
